version '0.1.0'
dependencies {
    compile libpacks.spring_common
    compile libpacks.cloud_routing
    compile libpacks.cloud_config_client
    compile libpacks.cloud_eureka_starter
    compile project(':commons')
}