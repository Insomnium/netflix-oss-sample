server:
  port: ${CONFIG_PORT:8888}

# see http://localhost:8888/admin/env

spring:
  cloud:
    config:
      server:
        git:
          uri: git@bitbucket.org:ins-ms/infra-configs.git

encrypt:
  key-store:
    location:   file:${JKS_DIR:/}config-server.jks
    password:   storepass123
    secret:     keypass123
    alias:      config-server-key
