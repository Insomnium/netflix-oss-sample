version = '0.0.1'

dependencies {
    compile libpacks.spring_common
    compile libpacks.cloud_common
    compile libpacks.groovy
    compile libpacks.spring_security
    compile libpacks.spring_oauth
}
