version = '0.0.1'

dependencies {
    compile project(':commons')
    compile libpacks.spring_common
    compile libpacks.cloud_common
    compile libpacks.spring_data_mongo
    compile libpacks.groovy

    testCompile libpacks.testing_mongo
}